name: Checks

on:
  push:
    branches: [main, master, dev]
  pull_request:
    branches: [main, master, dev]

jobs:
  checks:
    uses: zondax/_workflows/.github/workflows/_checks-golang.yaml@v3
    with:
      go_version: 1.24.4
      runner: zondax-runners
      enable_coverage: false
